---
- name: Provisioning role against chroot
  hosts: chroots
  connection: chroot
  user: root
  sudo: True
  vars_files:
    - [ "{{ buri_environment }}/local/overrides.yml" ]
  roles:
    - '{{ ami_role }}' image_build=True
