---
- name: Provisioning role against chroot
  hosts: chroots
  connection: chroot
  user: root
  sudo: True
  vars:
    image_build: True
  vars_files:
    - [ "{{ buri_environment }}/local/overrides.yml" ]
  roles:
    - '{{ ami_role }}'
