---
- name: Provisioning role against chroot
  hosts: chroots
  connection: chroot
  user: root
  sudo: True
  vars_files:
    - [ "{{ buri_environment }}/local/overrides.yml" ]
  vars:
    image_build: True
  roles:
    - "{{ ami_role }}"
